name: Node CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: env
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '10'
